# NEXT — toiito

セッション開始時にここを見る。
**タスクの台帳ではない**——作業単位と状態の正は GitHub Issues、順序と規約の正は `ROADMAP.md`。
ここに書くのは、 issue に載らない申し送りと、fermentary との受け渡しだけ。

## いま開いているもの

- **MVP の縦一本は実装として揃った**（2026-08-22）。
  #4（メモ UI）が PR #39 で入り、その中で #6（メモまわりの E2E）も通した。
  #1 / #2 / #3 / #5 / #11 / #42 と合わせて、ROADMAP の依存グラフのうち #7 の手前までが閉じている。
  E2E は 5 シナリオ（`web/e2e/dialogue.spec.ts` に 1 本、`memo.spec.ts` に 4 本）。
  残るは #7（MVP 到達判定・常用開始）で、実質は人間の判定
- **ただし #57 が縦一本を再訪のところで切っている**。
  「新しいセッションで再訪」を押すと `/q/[id]` が最新セッションだけを描くので、それ以前の発話が画面から消え、メモからの逆引きが着地しない。
  データは残っていて読む口が無いだけだが、常用の一巡（投入 → 対話 → メモ → 逆引き）は再訪を挟んだ時点で成立しない。
  **#7 の常用判定を #57 の前に始めるかは人間の判断**。
  #57 は先に設計を決める必要がある（問いの画面が何を見せるか・逆引きの着地先・「再訪」が文脈のリセットなのか区切りなのか）ので、決まるまで Claude は着手できない
- **メモ UI の手触りは #56 で別立て**。
  #4 は最小限で通してあり、選択直後のフォームのオーバーレイ化・下線ホバーでのメモ表示・重なった区間の下線は #56 へ送った。
  設計判断が要らないので、こちらはそのまま着手できる
- **開発ハーネスの別トラックは畳んだ**。
  #34（ブランチ保護）は ruleset `main: via PR + check green` を有効にして決着した。
  **main への直 push は塞がっている**ので、この NEXT.md の一行更新も PR を通る（申し送りにあった「許すか塞ぐか」の判断はこれで済んだ）。
  設定の中身と理由は `HARNESS.md`（GitHub 側の状態は diff に残らないので、あそこが正）。
  #20（公開時のトリガ制限）も入り、@claude の起動はリポジトリに招かれた人だけに絞った。
  **リポジトリは公開済み**——ruleset が enforce されるのもこれが前提
- **#58 は栞のまま**（ハーネス側で唯一残っているもの）。
  公開後、fork からの PR で `claude-code-review` が赤くなり続けるのを止める話。
  job レベルの `if:` で `head.repo.full_name` を見る設計案が issue のコメントに出ているが、**fork PR が実際に来る見込みが立つまで着手しない**（来ないうちに条件を足しても緑にも赤にもならず、検証できない）
- **#8 / #9 / #10 は #7 の後**（post-mvp ラベル）。
  常用が始まって実測が出るまで開けない
- **並行で走らせる規律**（2026-08-20 に実測）。
  触るファイルが互いに素なら同時に流してよい。
  ただし **#56 と #57 は並行させない**——前者は `message-body.tsx` の下線とリンク先、後者は `/q` のルートと `/memos` のリンク先を触り、`#msg-` の綴りを共有する三箇所で必ず重なる。
  テストの隔離は #33 で入ったので、新しいテストは「ケースごとに DB が空」を前提に書く（共有 DB を避けるための細工を書かない）

## 申し送り

- **[Claude 向け] main へは PR 経由でしか入らない**。
  ruleset で直 push を塞ぎ、`check` の緑と main の取り込み（Update branch）を必須にしてある。
  リモートのセッションはブランチを push するところまでで、マージは人間が押す

- **[Claude 向け] `.env*` は権限設定で Claude が触れない**。
  接続先の追記は済んでいる（2026-08-16 に人間が実施）。
  以後 env に何か足す必要が出たら、自分で書かず人間へ渡すこと。
  変数の一覧と意味は `web/README.md`。
  リモート（Claude Code on the web）だけは例外で、`.env.local` はセッション起動フックが書く（手元には存在しないファイルなので、上書きの心配は無い）

- **[Claude 向け] リモートは docker 無しで動く**。
  `.claude/hooks/session-start.sh` が Postgres・toolchain・依存・migration を用意するので、`docker compose up -d` を探しに行かない。
  非対称（Postgres 16・mise 直置き）は `HARNESS.md`「リモート」が正

- **[Claude 向け] 手元の DB は空**（#11 で SQLite からの移送を取りやめた。2026-08-16 決定）。
  一覧が空でも壊れているわけではない。
  #3 が入ったので `docker compose up -d` → `pnpm seed` で開発用の一式が入る（自分の問いで埋めたいなら `pnpm dev`）。
  旧 SQLite の実体は `web/data/`（git 管理外）に残っているので、S0 の対話を読み返す手はある

- **[人間向け] E2E の実走は手元（macOS）**。
  初回だけ `web/` で `pnpm exec playwright install chromium` が要る（ブラウザの実体は `pnpm install` では入らない）。
  リモートは cdn.playwright.dev へ出られないのでこれができず、L4 だけは手元でしか緑にできない。
  いま 5 シナリオある

- **[人間・任意] メモの手触り確認**。
  `pnpm seed` → `TOIITO_FAKE_AI=1 pnpm dev` で、発話を選んでメモを付け、`/memos` から逆引きして戻るところまで。
  下線の太さ・ホバーの出方・フォームの位置は官能でしか決まらないので、違和感が出たら #56 へ足す

- **[人間・任意] ペルソナ改定の体感確認**。
  S0b で二体に手筋のカタログと材料の供給規律を入れたが、効きは L5（人間の官能）でしか測れない。
  `pnpm dev` で自分の問いを一件投げてみて、手応えが薄ければ `web/src/personas/*.md` を直接編集してよい（プロンプトは文書として管理する方針。
  コード変更は不要）。
  どの issue の前提でもない

## fermentary との受け渡し

- セッション開始時に fermentary `NEXT.md` の自分宛（`[→toiito]`）搬送メモを確認し、拾ったら消す。
  宛先が自分でないメモは実行も削除も編集もしない
- 常用中に出た違和感は膜の一行判定で振り分ける（アプリ改善ネタ → issue、概念的な学び → fermentary の inbox/questions）

### 未搬送（リモートで出た素材。次の手元セッションが膜の判定を通して搬入する）

- **[未搬送 → `ATLAS.md` terrarium 節の toiito 行]** 2026-08-22 に節目が二つ来た。
  MVP の縦一本が実装として揃い（#1〜#6 と #42 が完了、残るは #7 の常用判定）、リポジトリを公開へ切り替えた。
  status が実態と乖離しているので、台帳の行を直す

- **[未搬送 → `questions.md` 候補]** 「対話を再訪するとは、何を捨てて何を持ち越すことか」。
  #57 で、再訪が文脈のリセットなのか単なる区切りなのかを決めないと画面の設計が決まらないと判明した。
  toiito の実装判断としては issue 側で閉じるが、問いそのものは AI との対話一般に効くので膜の判定にかける価値がある

## 文書の地図

| 文書 | 役割 |
|---|---|
| `VISION.md` | なぜ作るか（最上位） |
| `ARCHITECTURE.md` | どう作るか（スキーマ・オーケストレーションの正） |
| `HARNESS.md` | どう検証するか（L0〜L5・フェイクモード・ブランチ保護） |
| `ROADMAP.md` | どの順で作るか（依存グラフ・横断規約・MVP 完了条件） |
| GitHub Issues | 何を作るか + 状態（実行単位の正） |
| `PLAN-rationale.md` | なぜそう決めたか（経緯層。着手時に読む必要はない） |
| `extensions/` | MVP 後に解凍する構想（graph-view / fermentation-and-outlets） |
