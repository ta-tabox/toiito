# DESIGN — 見た目と使用感の正

2026-08-31 起草（issue #66「DESIGN.md を作り、トンマナと使用感を定義する」）。
上位文書: `VISION.md`（なぜ）> `ARCHITECTURE.md`（どう）> 本文書（どう見えるか）。
矛盾したら上位が勝つ。

本文書はコードを持たない。
実画面へ当てはめるのは #67（既存画面を DESIGN.md へ寄せる）で、そこが唯一この文書を消費する場所になる。

## 原則

`VISION.md`「設計原理」の四つを、画面の言葉へ翻訳したもの。
迷ったらここへ戻る。

1. **速度を KPI にしないので、画面は待ちを隠さない**。
   進捗バーもスケルトンも置かない。
   速く見せる演出は、速さを目的に格上げする
2. **具体と抽象を往復させるので、三者は同じ幅で並ぶ**。
   二体のどちらかを主に、他方を従にしない
3. **自己参照的リンキングが第一級なので、自分が付けた印が本文で最も強い**。
   AI の発話より、そこへ引いた線の方が目に入る
4. **対話が資産として堆積するので、過去は画面から消えない**。
   読み返す口を常に持つ

## 語彙

**語の正は `VISION.md`「語彙」節**。
工程名・画面のアクション名・`status` のラベルはすべてそこから引く。
本文書はここで重複させない。

本文書が決めるのは、その語を**どの大きさ・どの色・どの位置に置くか**だけである。

## 色

出所は 2026-08-31 に人間が起こした案（Fermented Inquiry / Fermented Inquiry Dark）。
中心メタファーの発酵槽（ADR-0016）を、苔と生成りの側で具体化したもの。

### トークン

| 用途 | light | dark |
|---|---|---|
| 地 | `#fbf9f4` | `#121410` |
| 面（低） | `#f5f3ee` | `#1a1c18` |
| 面（中） | `#f0eee9` | `#1e201c` |
| 面（高） | `#eae8e3` | `#292b26` |
| 文字 | `#1b1c19` | `#e3e3dc` |
| 文字（弱） | `#434840` | `#c3c8bc` |
| 罫 | `#c3c8bd` | `#43483f` |
| 緑 | `#334f2b` | `#afd0a1` |
| 緑（塗り） | `#4a6741` | `#324e2a` |
| 緑の上の文字 | `#ffffff` | `#e3e3dc` |
| 面（暖） | `#f4efe6` | `#201d17` |
| 面（寒） | `#e8ebe4` | `#1b201b` |
| 琥珀 | `#9a7223` | `#c9a557` |

面（暖）・面（寒）・琥珀の三つだけは案の外で足した。
前二つは話者を分けるための派生で、琥珀はメモの印に要る色である（案の側にメモという概念が無い）。

### 彩度の規律

**面は彩度を持たない。**
**暖と寒の傾きだけを持つ。**
彩度を持つのは二つだけで、**人間の発話（緑の塗り）**と、**自分が付けた印（琥珀の線）**である。

理由は原則 3 にある。
面に色を配ると、色数が増えるほど印が沈む。
彩度を人間の手が触れた二箇所に限れば、画面の中で最も強い色は必ず自分が置いたものになる。

この規律は検査できる。
彩度のある面が三つ目に増えていたら、そこが破れ目である。

### 話者の描き分け

| 話者 | 位置 | 面 | 形 |
|---|---|---|---|
| あなた | 右 | 緑（塗り） | 角丸 14px、右上を 4px に落とす |
| 具体 | 左 | 面（暖） | 角丸 14px、左上を 4px に落とす |
| 抽象 | 左 | 面（寒） | 角丸 14px、1px の罫を回す |

暖が具体、寒が抽象。
現行の `bg-amber-50` / `bg-sky-50` が持っていた温度の対応をそのまま引き継ぐ。

二体は左に並ぶので、位置では分かれない。
**分けるのは温度と枠の形**で、どちらも彩度を使わない。
温度差は周辺視で拾う手掛かりであって識別の正ではないので、名前のラベル（`具体` / `抽象`）は消さない。

## タイポ

**和文が本体である。**
欧文書体は和文の字形を持たないので、欧文だけを指定した設計は日本語の画面に一文字も効かない（2026-08-31、案の HTML を実測。`EB Garamond` と `Manrope` の二つだけで和文の指定が無く、日本語は閲覧環境の既定ゴシックで出ていた）。

| 役 | 書体 | 用途 |
|---|---|---|
| 見出し | 明朝（`Shippori Mincho`） | 問いの本文、画面の見出し |
| 本文・UI | ゴシック（`Zen Kaku Gothic New`） | 発話、ラベル、ボタン、メタ情報 |

問いだけが明朝で立ち、対話はゴシックで読み下せる。
問いは何度も読み返すものなので字面が残ってよく、対話は流れるので読みやすさを採る。

| 要素 | モバイル | デスクトップ |
|---|---|---|
| 問い（見出し） | 20px / 行間 1.75 | 24px / 行間 1.7 |
| 発話の本文 | 16px / 行間 1.95 | 17px / 行間 1.95 |
| ラベル・メタ | 13px / 行間 1.7 | 13px / 行間 1.7 |
| 状態のピル | 12px | 12px |

行間は 1.9 を下回らない。
一行は全角 34 字を超えない（モバイルでは幅が先に効くので、実効は 20 字前後になる）。

## 余白と密度

8px を基調にする。
すべての間隔はその倍数で、半端な値を置かない。

| 場所 | 値 |
|---|---|
| 画面の左右（モバイル） | 20px |
| 本文の一列の最大幅 | 640px（中央寄せ） |
| 発話と発話の間 | 16px |
| 節と節の間 | 32px |
| 面の内側 | 12px（モバイル）／16px（デスクトップ） |

**密度を上げる方向の変更を既定で疑う**。
一画面に多く入れることは、このアプリでは利得にならない。

## 重心 — 読みの器と、書きの間

**既定は読み**。
入力欄は本文の下に細い一行で待ち、面も影も持たない。

**入力へ触れた瞬間だけ、書きの間へ移る**。
対話が退き（不透明度 .28）、入力が面と縁を得て主役になる。
書き終えるか、外を触れば読みへ戻る。

**常設の固定入力欄は置かない**。
常に手の届く入力欄は常に返答を促す形なので、沈黙を仕様として残す設計（後述）と正面から当たる。

## 状態の見せ方

### `status`（7 値）

淡いピルで出す。
**地は 7 値とも一律**（面（高））で、先頭の点だけが二値を持つ。

| 点 | 意味 | 値 |
|---|---|---|
| 緑 | 開いている | `new` `stocked` `holding` `permanent` |
| 無彩 | 閉じた | `resolved` `exported` `discarded` |

7 値を色で分けない。
分けた瞬間に一覧は処理待ちの列になり、`ARCHITECTURE.md`「意図的にやらないこと」の「問いの解決済みクローズフロー」を見た目の側から作ってしまう。
**開いているか閉じたかの一ビットだけ**なら、走査は速くなるが板にはならない。

ラベルの文字列は `VISION.md`「語彙」節が正で、実装の対応は `web/src/lib/question.ts`。

### 応答待ち

「綴られる」（後述）。
待っている間に別の指標を出さない。

### セッションの区切り

**#57（再訪とセッションの設計）で決着している。**
**DESIGN.md は選び直さない。**

区切りそのものは**強い**（過去は読み取り専用で、発話を足せない）。
痕跡は**弱く**添える（キーワードを 3 語まで、日付の脇に小さく）。
この非対称が「忘れているが、手掛かりは残っている」を作る。
両方を強くすると引き継ぎに見え、両方を弱くすると再訪で対話が消えたのと同じに戻る。

## 動き

**綴られる。**
AI の応答は、一字ずつ打つのではなく**行が上から置かれる**。
不透明度 0→1 と 7px の落差を 0.85s、行ごとに 0.34s ずらす。

一息に出る文字は「返信」だが、置かれていく文字は「書き込み」である。
これは体感の改善ではなく比喩の実装なので、`ARCHITECTURE.md`「快適さの最適化をしない」の適用対象ではない。

**本文は最初から DOM に置く**。
動かすのは不透明度と位置だけで、文字を差し込まない。
`web/src/components/message-body.tsx` が本文の文字オフセットでアンカーを持つので、描画の途中で文字を選んでもオフセットが狂ってはいけない（`e2e/memo.spec.ts` の 6 本がこの経路を守っている）。

ストリーミング（#9 / #10）が入るまでは一息に出してよい。
入れば、行が届いた順に置かれる。

**`prefers-reduced-motion` を尊重する**。
切っている人には、落差も遅延も無しで出す。

逆引きの着地（`data-landed`、2.5s）は現行のまま残す。
**注意が道具へ向く動きは下の線引きで直す側に入るので、ホバーで面を拡大しない**。

## 対応範囲

**スマホ第一**。
2026-08-29 に本番が立ち、実機での常用が始まっている。
判断は iPhone の幅で成立するかで行い、デスクトップは中央一列を広げるだけにする。

**ダークを持つ**（トークン二組）。
現状は地色だけが反転し、吹き出しが `bg-white` / `bg-amber-50` / `bg-sky-50` のまま固定されているので、半分だけの対応になっている。
#67 で二組へ組み直す。

## 部品の型

**共通部品を切る**（`web/src/components/ui/`）。
ダークで二系統になり、#68（ログイン）が導線を足すので、直書きだと同じ綴りが散る。

最初に切るのは四つ。

| 部品 | 何を持つか |
|---|---|
| `Button` | 実（緑の塗り）と淡（罫だけ）の二態 |
| `Field` | 入力欄。読みの一行と、書きの間の二態 |
| `Row` | 一覧の一行。問い・メモに共通 |
| `Pill` | 状態のピル。点の二値を持つ |

**UI ライブラリは入れない**。
既定が快適な SaaS の見た目であるものは、そこから離れるより寄せる方が楽になり、しかも当たったことに気付きにくい。

**五つ目からは閾値で決める**——同じ綴りが 3 箇所に出たら切る。
2 箇所では切らない（`CODING.md`「YAGNI」）。

## 残す摩擦

線引きの原則は**注意の向き先**である。

> 問いへ向かう注意を止めるものは残し、道具そのものへ注意を奪うものは直す。

待ち時間は問いへの注意を切らないので残る。
引っかかりは道具が気になるので直る。
体験改善の要求が来たら、毎回この一行に当てる。

| 残す（仕様） | 直す（妨害） |
|---|---|
| AI の応答待ち 15〜27 秒 | 何が起きたか分からない沈黙 |
| 二体が必ず両方喋る | 途中で失敗して欠けた turn が残る（#119） |
| 過去のセッションへ発話できない | 呼び出しに上限時間が無い（#120） |
| 一覧に検索も絞り込みも無い | 選択のたびに発話の数だけ再計算が走る（#129） |
| 通知が無い | 選択箇所から目が離れるメモの入力欄（#56） |
| 「答えを出す」ボタンが無い | 実機で入力が引っかかる |

左の列は**削らない**。
削る提案が来たら、それは改善ではなく効果の除去である（`extensions/fermentation-and-outlets.md`「設計上の自己言及: 反快適性」）。

## やらないこと

- **状態を 7 色に分けること**。
  ピルの地は一律で、点だけが開／閉の二値を持つ
- **常設のサイドバー・タブバー**。
  画面は `/`・`/q/[id]`・`/memos` の 3 枚しかない
- **通知**。
  戻る動機を催促で作らない。
  再訪を起こすのは材料であって呼び出しではない
- **進捗インジケータの類**。
  発酵の進みを図形で出すと、進み具合そのものが目的になる（`ARCHITECTURE.md` の「KPI・ゲーミフィケーション」に当たる）
- **アバターと検索**。
  持ち主が一人の間は意味を持たない。
  #68 の後に必要が出たら、その時に決め直す
- **二体への固有名**。
  トイットの二体は `具体` と `抽象` であって、人格の名前を持たない（`web/src/lib/personas.ts`）。
  ペルソナを増やす・選ぶ話は #64 の領分
- **画面の英語**。
  文言は全部日本語で通す
- **速く見せる演出**。
  スケルトン・プログレスバー・楽観的更新
