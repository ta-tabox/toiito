/**
 * 発話本文の値域。
 *
 * 持つのは上限だけ。
 * 永続化は db.ts、一往復の手順は turn.ts の領分で、ここはその両方とクライアント側のフォームから引ける位置に置く。
 */

/**
 * 発話本文の長さの上限（UTF-16 code unit）。
 *
 * 単位を code unit に採るのは textarea の `maxLength` が同じ単位で数えるためで、ブラウザが止める位置とサーバーが弾く位置がずれない。
 *
 * これは摩擦として使う値ではない（`DESIGN.md`「残す摩擦」）。
 * 書いている途中で止まる上限は道具そのものへ注意を奪うので、通常の使用では当たらない高さに置く。
 * 止めたいのは暴走した貼り付けの方で、この値が無いと効いている境界は Next の `serverActions.bodySizeLimit` の既定（1MB）だけになる。
 */
export const MESSAGE_BODY_MAX_LENGTH = 4000;
