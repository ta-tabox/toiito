# ストリーミング化 spike（設計を書く）
labels: claude,design,post-mvp

**前提**: #7 ／ **担当**: Claude ／ **参照**: `ARCHITECTURE.md`, `PLAN-rationale.md` S5 節（実施理由の経緯）

## 目的
**この issue の成果物はコードではなく設計文書**。旧プランの S5 は「着手セッションの
冒頭で詳細化してから書く」という約束のまま実装ステップに置かれており、
そのままでは投げられなかった。設計と実装を割る。

## 前提の再確認（着手時に必ず行う）
S0 の実測は「一往復・浅い文脈で遅くない」。#7 の常用を経た**実測**で、
文脈が育ったときに実際に待たされたかを確認する。
- **待たされていないなら**: この issue を「見送り」で閉じてよい。
  ただしポートフォリオとしての見せ場という理由は残るので、その判断は明示的に書く
- **待たされているなら**: 以下を書く

## 書くもの（`extensions/streaming.md` として起草）
- `web/src/app/api/speak/route.ts` の SSE イベント設計
  （`ai_a 開始 → チャンク列 → ai_a 確定 → ai_b 開始 → …`）
- `claude.ts` の `callPersonaStream`（`AsyncIterable<string>`）のシグネチャ。
  **フェイクモードも数チャンクに割って流す**（E2E が検証できるように）
- Server Action の同期往復からの移行手順。**逐次性の契約は維持——並列化しない**
- DB 書き込みは**確定時に一括**（`messages` immutable を崩さない。途中経過を保存しない）
- 楽観的更新の範囲（人間発話のみ）
- #6 のシナリオ 1 をどう改修するか
- #8（発話周期の可変化）と競合する箇所の洗い出しと、着手順序の結論

## 完了条件（機械判定）
- `extensions/streaming.md` が存在し、上記の全項目に節がある
- 「見送り」で閉じる場合は、その判断と理由が同ファイルに記録されている

## 人間の判定（別トラック）
設計への合意。
