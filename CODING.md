# コーディング規約（核）

Clean Code / リーダブルコード / 良いコード悪いコードの交差点の蒸留。
判断に迷う具体例・言語別の作法は skill `coding-standards` が持つ（レビュー・リファクタ・設計判断時に展開される。
ここには常時必要な原則だけを置く）。

## 命名
- 名前は意図を語る。読み手が中身を推測せずに済む名前。誤解されない名前 > 短い名前
- 情報量はスコープに比例させる。ループ変数は `i` でよく、公開 API は冗長でよい
- 名前に嘘をつかせない。挙動が変わったら改名もセットで行う

## 関数・メソッド
- 小さく、一つのことを。説明に「〜して、〜する」が要るなら分割候補
- 抽象度を揃える。高レベルの流れと低レベルの操作を同一関数に混在させない
- 引数は少なく（3つ以上で構造化を検討）。フラグ引数は関数分割の合図
- 隠れた副作用を持たせない。副作用があるなら名前で告知する

## 制御流
- ガード節・早期 return でネストを浅く。else を減らす
- 条件式は肯定形・単純に。複雑な条件は説明変数か関数へ抽出

## 状態・データ
- 不変を既定にする。可変は必要な場所へ最小スコープで
- 生成時に完全な状態を保証する（完全コンストラクタ）。不正な状態を表現不能にする
- データと、それを扱うロジックを引き裂かない。
  あるデータのバリデーション・計算が外部（Manager / Service 等）に散っていたら、データを持つ側へ寄せる（Data Class 臭）。
  ※ 構文の話ではない——`@dataclass` 等は不変な値オブジェクトの手段としてむしろ推奨。
  ロジックを持たない運搬役が正しい境界の DTO は例外

## エラー
- 例外を握りつぶさない。null を返さない・渡さない（空コレクション / Optional / 例外で代替）
- エラーは発生源の近くで、文脈を付けて扱う

## コメント
- コードで表現できることはコードで。コメントは why（意図・制約・経緯）専用
- コメントで補う前に、名前と構造を直せないか先に問う

## テスト
- 実装ではなく振る舞いをテストする。テストしにくさは設計の悪臭
- 1 テスト 1 観点。テストコードも本体と同じ可読性規約に従う

## 全体
- YAGNI: 推測で汎用化しない。抽象は 2〜3 例目が現れてから抽出する
- DRY は「知識」の重複にのみ適用。偶然似ているだけのコードは統合しない
- ボーイスカウトルール: 触れたコードは触れる前より少し良くして返す
