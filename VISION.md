# VISION — トイット（Toiito）

立ち上げ時（2026-07-06）に意味構築した全体像の正。
システムアーキテクチャはここには書かない。
それはこのプロジェクトの初回以降のセッションで、この文書を土台に起こす。

## 一行

問いをポイっと仕込んで寝かせる発酵槽。
自分の問いと向き合うことを AI が支える Web アプリケーション。

## 問題意識（なぜ作るか）

AI はもっぱらスピードアップ（速く答えに到達する）の文脈で語られる。
しかし本当に価値があるのは逆方向。
**スローダウン**のために AI を使うこと。
自分の問いを深め、安易な答え・一般論・暫定解に逃げないための道具。
トイットはこの問題意識の具体化であり、反例の実装である。

## 中心メタファー: 発酵槽 / 醸造

- 問いは「解決すべきチケット」ではなく「仕込んで寝かせる原料」
- 仕込みは気軽に（ポイっと）、熟成は時間をかけて
- 発酵の触媒が AI との対話とリンキング
- **仕込んで終わりではない**。
  培地を整え、攪拌し、蒸留するところまで人間が関与する。
  この関与が、装置の名前の側から支えられている（発酵槽は中を覗き、温度を見て、攪拌するもの）
- 発想の出所は、手元で運用している発酵型の個人知識基盤。
  ただしトイットは **問いに特化した対話の場**であり、あちらは領域横断の記憶基盤（道具と住処の関係。混ぜない）

この比喩を選んだ理由と、採らなかった案（コンポスター維持 / 比喩を捨てる / ノート・日記）は `docs/adr/0016-central-metaphor-brewing.md`。

## 語彙

比喩の正が上の節、**語の正がここ**。
画面の文言（#67 の DESIGN.md）も `status` のラベルも、ここから引く。

器は**発酵槽**。
画面では「問いの発酵槽」（見出しと戻りリンク）。

### 工程

| 工程 | 何が起きる段か | 画面のアクション名 | 実装 |
|---|---|---|---|
| 仕込み | 問いを入れる | 「仕込む」（ボタン）／「問いをポイっと」（placeholder） | 実装済み・語は未反映 |
| 培地 | 材料が後から付く | —（画面に無い） | 未実装（#63） |
| 発酵 | 材料が付いた状態で寝ている | —（`status` に現れる） | `status` のみ |
| 攪拌 | 二体と議論して視点を混ぜる | 「発話する」（据え置き）／「攪拌する」（候補） | 実装済み |
| 熟成 | 時間をかけて寝かせる | —（画面に無い） | 未実装 |
| 蒸留 | 仮答えか次の問いへ落とす | —（`status` の変更として現れる） | 未実装 |
| 出口 | 結晶・持続・棄却へ分かれる | `status` のラベル | 実装済み |

**工程名と手触りを別の層に置く**。
ボタンは工程名（仕込む）、placeholder は手触り（ポイっと）で、後者が問いを投げるときの敷居を下げる。

**中立な語は寄せていない**（発話する・メモ・再訪）。
候補は上の表に載せたが、画面へ出すかは #67 で実画面を見ながら決める。
比喩は入口と器の名前に留め、対話の最中は中立語で通す形が既定。

画面の文言を実際に寄せるのは #67（既存画面を DESIGN.md へ寄せる）。

### `status` のラベル

**`status` の値そのものは比喩を持たない**。
比喩は選び直しうるが enum の変更は migration を伴うので、変わる側を表示層へ閉じてある（`docs/adr/0017-status-value-set.md`）。
比喩が動いてもこの表のラベル列だけが動き、値の列は動かない。

意味の正は `ARCHITECTURE.md`「問いの状態機械」、値域の正は `prisma/schema.prisma` と `web/src/lib/question.ts`。

| status | ラベル | 意味 |
|---|---|---|
| `new` | 仕込み中 | 仕込んだが、まだ材料が付いていない |
| `stocked` | 発酵 | 材料が付き、蒸留に入れる |
| `resolved` | 一旦閉じた | 答えが出て閉じた。別の置き場へは書き出していない |
| `exported` | 結晶した | 答えが出て、別の置き場へ書き出した |
| `holding` | 持ち続ける | 答えが出ないことは欠陥ではない |
| `permanent` | 閉じない問い | 閉じないことが正しいので、閉じ候補として催促しない |
| `discarded` | 棄却 | — |

## AI の役割 — 否定形で定義する（最重要の設計制約）

AI がやらないことが、このアプリのアイデンティティ:

- **答えを与えない**。
  一般論や暫定的な答えを提供しない
- 問いの**深掘りを支える**（掘るのは人間）
- **リンキングを支える**: 関連トピック、過去に自分が持っていた問いとの接続を提示する

「サポートするが代行しない」。
答えの供給者ではなく、発酵の環境条件。

## コア相互作用（3つ）

### 1. 問いの投入と対話

問いを投げ、それをベースに AI と対話するインターフェース。

### 2. 二対一の対話（超相対性理論モデル）

一対一ではなく **AI 二体 + 人間一人** の鼎談。
二体の AI に異なる性格・視点を定義づけ、思考が深く、**具体と抽象を往復**できるようにする。
構成の下敷きはポッドキャスト『超相対性理論』。
一対一だと対話が「質問→回答」の往復に収束しやすいのに対し、三者だと視点の衝突と転位が生まれる。
これが発酵の攪拌に相当する。

### 3. キーワードメモと逆引き

- 対話中に出てきたキーワードに対し、**文字選択 → メモ**を残せる
- メモが付いた文字はアンダーライン（リンク風スタイル）で可視化
- メモ一覧側から対象スレッド（セッション）を**逆引き**でき、どのような対話だったか後から振り返れる
- 対話は流れて消えるものではなく、堆積して振り返れるもの（発酵の観察窓）

## 設計原理（アーキテクチャ以前の不変条件）

1. 答えの供給ではなく問いの熟成を最適化する。
   速度・効率を KPI にしない
2. 具体と抽象の往復運動を UI と AI 人格設計の両方で担保する
3. 過去の自分の問いとの接続（自己参照的リンキング）が第一級の機能
4. メモ・逆引きにより対話が資産として堆積する

## 開いた問い（初回セッション以降で詰める）

- 二体の AI の性格・視点をどう定義するか（固定ペルソナか、問いに応じて可変か）
- 「発酵した」状態の判定・表現（時間経過? 再訪回数? 人間の宣言?）
- リンキングの実装粒度（キーワードベース? 埋め込みベース?）
- 手元の個人知識基盤が持つ問いの記録との関係（重複させるか、いずれ接続するか）
