# ROADMAP — MVP 完遂までの地図

2026-08-02 起草（前身: `PLAN.md`。
経緯は `PLAN-rationale.md` へ退避）。
上位文書: VISION.md（なぜ）> ARCHITECTURE.md（どう）> HARNESS.md（検証）> 本文書（順序）。
矛盾したら上位が勝つ。

## この文書の分担

| 層 | 器 | 持つもの |
|---|---|---|
| 地図 | **本文書** | 横断規約・依存グラフ・順序の理由・完了条件の閾値 |
| 実行単位 | **GitHub Issues** | 目的 / 作るもの / 触らないもの / 完了条件 / 参照文書 |
| 引き継ぎ | `NEXT.md` | 開いている issue 番号・fermentary 搬送メモ・申し送り |
| 経緯 | `PLAN-rationale.md` | 完了済みステップの帰結・方針改定の理由 |

**状態（着手中・完了）の正は issue 側**。
本文書はステップの ✅ を持たない—— 複数マシンから同じ markdown の状態を触ると、意味のない衝突を量産するため。
本文書が変わるのは順序か規約が変わったときだけ。

## 進め方の横断規約（全 issue 共通）

- 心拍: 変更 → `web/` で `pnpm check` → 緑ならコミット。**赤のままコミットしない**
- 作る順: **lib 関数 + テスト → UI 配線**（HARNESS.md 設計制約 4）
- AI を伴う動作確認は `TOIITO_FAKE_AI=1`。実 API は自動テストで叩かない
- 1 issue = 1 セッション = 1 PR。PR 本文に `closes #N`。CI 緑 + マージで閉じる
- コミット規約・署名は `CLAUDE.md` の git 節が正（prefix は変更の型、author は人間名義）
- セッション終了時: 開いている issue 番号を `NEXT.md` に反映 → コミット

## 完了条件の判定を二本に分ける

各 issue は二つの節を持つ:

- **完了条件（機械判定）** — `pnpm check` 緑・特定のテストが通る、など。
  これを満たせば issue は閉じてよい
- **人間の判定（別トラック）** — 実機の手触り、L5 の官能。
  **close を妨げない**。
  滞留したら独立の issue を立てる

一つのチケットに両方の判定を載せると、誰も閉じられない単位ができる。

## 依存グラフ

```
#1 メモ lib 層 ── #11 Prisma / Postgres 一本化 ─┬─ #3 シード ──┐
                                                 │              ├── #4 メモ UI ──┐
#2 ローディング表示 (独立)                       │                               ├─ #6 E2E ── #7 MVP判定/常用開始
                                                 └─ #5 メモ一覧・逆引き ─────────┘                    │
                                                                                                       ├── #8 発話周期の可変化
                                                                                                       └── #9 spike ── #10 実装
```

| # | 内容 | 担当 | 前提 |
|---|------|------|------|
| 1 | メモ lib 層（repo 関数 + anchors 純関数 + テスト） | Claude | — |
| 2 | 発話フォームのローディング表示 | Claude | — |
| 11 | **Prisma 導入 + Postgres 一本化**（割り込み） | Claude + 人間 | #1 |
| 3 | 開発用シードスクリプト `pnpm seed` | Claude | #11 |
| 4 | メモ UI（アンダーライン描画 + 選択 → メモ作成） | Claude | #11, #3 |
| 5 | メモ一覧と逆引きページ | Claude | #11（#4 と順不同） |
| 6 | Playwright E2E（3 シナリオ） | Claude + 人間 | #3, #4, #5 |
| 7 | MVP 到達判定・台帳更新・常用開始 | 人間 + Claude | #1〜#6 |
| 8 | 発話周期の可変化（具体／抽象のバランス） | Claude | #7 |
| 9 | ストリーミング化 spike（設計を書く） | Claude | #7 |
| 10 | ストリーミング実装（SSE） | Claude | #9 |

## 順序の理由

**lib → UI**。
オフセット演算（サロゲートペア・区間の重複・境界）は MVP 最大のリスク箇所で、DOM に埋めると検証できない。
純関数へ寄せてテストで固めてから配線する。

**#11（Prisma 化）を #3〜#5 の前に割り込ませる**（2026-08-15 追加）。
永続化を `node:sqlite` の生 SQL から Prisma + Postgres へ移すと、repo 関数が同期から `async` へ変わる。
UI を先に書けば配線をもう一度やり直すことになるので、UI が乗る前に土台を入れ替える。
なお `anchors.ts` は DB 非依存の純関数なので影響を受けない——lib を純関数へ寄せた設計がここで効いている。

**シードを UI より前**（旧プランでは E2E と同梱だった）。
シードがあれば #4/#5 の手触りをフェイクモードで即座に確かめられる。
E2E の前提でもあるので、早く置くほど得。

**#7（常用開始）を #9/#10（ストリーミング）より前**。
旧プランは逆順だった。
S0 の実測は「遅くない」で、遅くなるという予測は文脈が育つ前提の推論にすぎない。
常用を先に始めれば、必要性が実測で裏付く（あるいは裏付かない）。
ポートフォリオとしての見せ場という理由は正当だが、**常用開始を遅らせる理由にはならない**。

**#8（発話周期）を常用後**。
「掘りすぎ」の適切さは L5 でしか測れず、今の材料は一回の体感しかない。
メモ機能が入れば違和感そのものが逆引きできるデータになる——観測器を先に立ててから調律する（ARCHITECTURE「破られたら人間が観察できることが検知機構を兼ねる」の実体化）。

## MVP 完了条件

CLAUDE.md の完了条件「MVP が動き、自分の問いで実際に常用できている」の後半に、観測可能な閾値を置く（#7 で判定。
**提案値。
人間が調整してよい**）:

- 常用開始から **2 週間**で、実キーで**自分の本物の問い 3 件以上**を投入した
- 各問いで **3 往復以上**の対話が成立した
- メモが合計 **10 件以上**堆積した
- うち **1 件以上**を、後日 `/memos` から実際に逆引きして辿り直した

四つとも満たせば MVP 到達とし、以後は常用のフィードバック駆動へ移る。
満たせないなら、足りないのは実装ではなく設計なので、その旨を `PLAN-rationale.md` に記録して問いを fermentary へ搬送する。

terrarium の手仕舞いは `~/vivarium/fermentary/playbooks/terrarium.md` が正典。
