# ROADMAP — MVP 完遂までの地図

2026-08-02 起草（前身: `PLAN.md`）。
上位文書: VISION.md（なぜ）> ARCHITECTURE.md（どう）> HARNESS.md（検証）> 本文書（順序）。
矛盾したら上位が勝つ。

## この文書の分担

| 層 | 置き場 | 持つもの |
|---|---|---|
| 地図 | **本文書** | 横断規約・依存グラフ・順序の理由・完了条件の閾値 |
| 実行単位 | **GitHub Issues** | 目的 / 作るもの / 触らないもの / 完了条件 / 参照文書 |
| 引き継ぎ | `NEXT.md`（手元のみ。追跡しない） | 開いている issue 番号・申し送り |
| 経緯 | `docs/adr/` | なぜそう決めたか。1決定1レコード・追記のみ・覆すときは supersede |

**状態（着手中・完了）の正は issue 側**。
本文書はステップの ✅ を持たない—— 複数マシンから同じ markdown の状態を触ると、意味のない衝突を量産するため。
本文書が変わるのは順序か規約が変わったときだけ。

## 進め方の横断規約（全 issue 共通）

- 心拍: 変更 → `web/` で `pnpm check` → 緑ならコミット。**赤のままコミットしない**
- 作る順: **lib 関数 + テスト → UI 配線**（HARNESS.md 設計制約 4）
- AI を伴う動作確認は `TOIITO_FAKE_AI=1`。実 API は自動テストで叩かない
- 1 issue = 1 セッション = 1 PR。PR 本文に `closes #N`。CI 緑 + マージで閉じる
- コミット規約・署名は `CLAUDE.md` の git 節が正（prefix は変更の型、author は人間名義）
- セッション終了時: 開いている issue 番号を `NEXT.md` に反映 → コミット

## 完了条件の判定を二本に分ける

各 issue は二つの節を持つ:

- **完了条件（機械判定）** — `pnpm check` 緑・特定のテストが通る、など。
  これを満たせば issue は閉じてよい
- **人間の判定（別トラック）** — 実機の手触り、L5 の官能。
  **close を妨げない**。
  滞留したら独立の issue を立てる

一つのチケットに両方の判定を載せると、誰も閉じられない単位ができる。

## 依存グラフ

```
#1 メモ lib 層 ── #11 Prisma / Postgres 一本化 ─┬─ #3 シード ─┬─ #42 E2E 足場 ─┐
                                                 │             │                 │
#2 ローディング表示 (独立)                       │             └─ #4 メモ UI ────┼─ #6 E2E 残り ── #7 MVP判定/常用開始
                                                 │                               │                        │
                                                 └─ #5 メモ一覧・逆引き ─────────┘                        ├── #8 発話周期の可変化
                                                                                                           └── #9 spike ── #10 実装
```

| # | 内容 | 担当 | 前提 |
|---|------|------|------|
| 1 | メモ lib 層（repo 関数 + anchors 純関数 + テスト） | Claude | — |
| 2 | 発話フォームのローディング表示 | Claude | — |
| 11 | **Prisma 導入 + Postgres 一本化**（割り込み） | Claude + 人間 | #1 |
| 3 | 開発用シードスクリプト `pnpm seed` | Claude | #11 |
| 4 | メモ UI（アンダーライン描画 + 選択 → メモ作成） | Claude | #11, #3 |
| 5 | メモ一覧と逆引きページ | Claude | #11（#4 と順不同） |
| 42 | Playwright の足場（webServer・e2e 用 DB・スクリプト・シナリオ 1） | Claude + 人間 | #3 |
| 6 | Playwright E2E（メモまわりの 2 シナリオ） | Claude + 人間 | #4, #42 |
| 7 | MVP 到達判定・常用開始 | 人間 + Claude | #1〜#6 |
| 8 | 発話周期の可変化（具体／抽象のバランス） | Claude | #7 |
| 9 | ストリーミング化 spike（設計を書く） | Claude | #7 |
| 10 | ストリーミング実装（SSE） | Claude | #9 |

## 順序の理由

**lib → UI**。
オフセット演算（サロゲートペア・区間の重複・境界）は MVP 最大のリスク箇所で、DOM に埋めると検証できない。
純関数へ寄せてテストで固めてから配線する。

**#11（Prisma 化）を #3〜#5 の前に割り込ませる**（2026-08-15 追加）。
永続化を `node:sqlite` の生 SQL から Prisma + Postgres へ移すと、repo 関数が同期から `async` へ変わる。
UI を先に書けば配線をもう一度やり直すことになるので、UI が乗る前に土台を入れ替える。
なお `anchors.ts` は DB 非依存の純関数なので影響を受けない——lib を純関数へ寄せた設計がここで効いている。

**シードを UI より前**（旧プランでは E2E と同梱だった）。
シードがあれば #4/#5 の手触りをフェイクモードで即座に確かめられる。
E2E の前提でもあるので、早く置くほど得。

**#6 を足場（#42）と残り（#6）へ割る**（2026-08-20 追加）。
3 シナリオのうち #4 に依存するのはシナリオ 2 と、シナリオ 3 の着地だけで、Playwright の導入・e2e 専用 DB・スクリプト整理・シナリオ 1 は #4 と無関係に書ける。
#4 は MVP 最大の面積を持ち滞留しやすいので、依存しない配管を先に敷いて待ち時間を作らない。
分割の副産物として、E2E の足場が壊れたのか実装が壊れたのかを PR 単位で切り分けられるようになる。

**#7（常用開始）を #9/#10（ストリーミング）より前**。
旧プランは逆順だった。
S0 の実測は「遅くない」で、遅くなるという予測は文脈が育つ前提の推論にすぎない。
常用を先に始めれば、必要性が実測で裏付く（あるいは裏付かない）。
ポートフォリオとしての見せ場という理由は正当だが、**常用開始を遅らせる理由にはならない**。

**#8（発話周期）を常用後**。
「掘りすぎ」の適切さは L5 でしか測れず、今の材料は一回の体感しかない。
メモ機能が入れば違和感そのものが逆引きできるデータになる——観測手段を先に立ててから調律する（ARCHITECTURE「破られたら人間が観察できることが検知機構を兼ねる」の実体化）。

## MVP 完了条件

CLAUDE.md の完了条件「MVP が動き、自分の問いで実際に常用できている」の後半に、観測可能な閾値を置く（#7 で判定。
**提案値。
人間が調整してよい**）:

- 常用開始から **2 週間**で、実キーで**自分の本物の問い 3 件以上**を投入した
- 各問いで **3 往復以上**の対話が成立した
- メモが合計 **10 件以上**堆積した
- うち **1 件以上**を、後日 `/memos` から実際に逆引きして辿り直した

四つとも満たせば MVP 到達とし、以後は常用のフィードバック駆動へ移る。
満たせないなら、足りないのは実装ではなく設計なので、その旨を `docs/adr/` に記録して設計から問い直す。

## MVP 後の第二波（2026-08-22 追加）

常用の手前で出た要求を issue へ割った一群。
着手は #7（常用開始）の後を既定とする。
**#61 / #66 は文書だけなので、走っている実装とは衝突しない**——コードを触らないため。
ただし衝突しないことと着手できることは別で、どちらも #57（再訪とセッションの設計）の決定に従属する。

```
#57 再訪とセッションの設計 ── #61 比喩と語彙 ── #66 DESIGN.md ─┬─ #67 既存画面を寄せる
     └─ #57 は #66 へも直接効く                                  └─ #56 メモ UI の手触り
        （決定 1「画面が何を見せるか」→ 決定 4「区切りの見せ方」）    （#67 とは並行させない）

#62 派生問い（question_links）   ← #57 と同じ /q を触るので並行させない
#63 培地（cultures）
#64 ペルソナ設定 ── #75 ペルソナの記憶（#57 の決定 3 にも従属）
     ↑ #64 は #8 と隣接（あちらは「いつ」、こちらは「どう」）

#71 実行環境を決める ── #90 本番へデプロイ ─┐
                                            ├─ #68 ログインと所有権 ─┬─ #69 管理機能
#65 認証方式を決める ───────────────────────┘                        └─ #70 BYOK
```

| # | 内容 | 担当 | 前提 |
|---|------|------|------|
| 61 | 中心メタファーを実際の工程へ合わせ直し、語彙を確定する | 人間 → Claude | #57 |
| 66 | DESIGN.md を作り、トンマナと使用感を定義する | 人間 → Claude | #61, #57 |
| 67 | 既存画面を DESIGN.md へ寄せる | Claude | #66 |
| 56 | メモ UI の見せ方を詰める（オーバーレイ・ホバー・重なりの下線） | Claude | #66 |
| 62 | 問いから派生問いを立て、前後を辿れるようにする | Claude | — |
| 63 | 培地を整える（対話の前に AI が材料を寄せる） | 人間 + Claude | — |
| 64 | 二体のペルソナをセッション単位で設定できるようにする | Claude + 人間 | — |
| 75 | ペルソナに、セッションを跨いだ記憶を持たせる（何を忘れるかの設計） | 人間 → Claude | #57, #64 |
| 71 | アプリケーションの本番実行環境を決める | 人間 + Claude | — |
| 90 | 本番環境へデプロイする（Vercel + Neon） | 人間 + Claude | #71 |
| 65 | 認証と所有権の方式を決める | 人間 | — |
| 68 | ログイン（Google OAuth）とリソースの所有権 | Claude + 人間 | #65（公開するなら #90 も） |
| 69 | 管理機能（利用状況の把握とアカウントの停止） | Claude + 人間 | #68 |
| 70 | ユーザーごとのモデル選択と API キー（BYOK） | 人間 + Claude | #68 |

### 順序の理由

**設計が先、比喩が後**。
「新しいセッションで再訪」が何を意味するか（#57 の決定 3）が決まらないと、その呼び名（#61 の決定 3）は決められない。
セッションの区切りが画面に出るかどうか（#57 の決定 1）も、区切りをどの強さで見せるか（#66 の決定 4）の前提になる。
意味が先、呼び名が後。

**比喩が先、デザインが後**。
画面の言葉は比喩から降りてくるので、逆順でやると決めた直後のトンマナを言い換えで塗り直すことになる。

**#56（メモ UI の手触り）は #66 の後**。
下線の太さと色は DESIGN.md が決めるトークンから降りてくるので、先にやると官能の調整を二度やることになる。
#4 が最小限で通したものを引き取る issue で、設計判断は要らない。

**#75（ペルソナの記憶）は #64 の後**。
記憶はペルソナ ID に紐づくので、ID を選べる形が先に要る。
加えてこの issue の核は実装ではなく「何を忘れるか」の設計で、#57 の決定 3（適度に忘れる）が要求そのものになっている。
全部覚えさせると #57 が落とした「文脈の引き継ぎ」に戻り、前回の結論を再生産する相手は問いを閉じる方向へ働く（VISION の制約と正面から当たる）。

**認証は決めてから実装**。
所有権の粒度と既存データの移行の書き方が方式に依存するので、後から方式が動くと migration をやり直すことになる。
加えて `ARCHITECTURE.md`「意図的にやらないこと」がマルチユーザーの作り込みを禁じているため、改定しないまま実装へ進むと規約違反のまま進むことになる。

**培地（#63）と派生問い（#62）は常用の後**。
どちらも出所は `extensions/` の構想で、着手判断は「MVP 常用後」と書いてある。
材料が溜まる前に受け皿だけ作っても、効いたかどうかを測れない。

**#71 → #90（実行環境）は他人を入れるより前**。
実行環境が無いと #68 が公開する先を持たない。
常用そのものにも効く——手元の `pnpm dev` に縛られている間は、思いついた場所から問いを投げられない。
決定（#71）と実作業（#90）を割ってあるのは、前者が `pnpm check` で閉じるのに対し、後者はアカウントと本番 URL が要って機械判定に載らないため。

**#69 と #70 は #68 の後で並行**。
どちらもユーザーが居ないと意味を持たず、互いには依存しない。
